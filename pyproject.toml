[tool.uv.workspace]
members = ["client", "schema", "server"]

[tool.pytest.ini_options]
# importlib mode lets same-named test modules in different members coexist
# without per-directory __init__.py files.
addopts = "--import-mode=importlib"
