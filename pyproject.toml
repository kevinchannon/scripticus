[tool.uv.workspace]
members = ["client", "schema", "server"]

[tool.pytest.ini_options]
# Both members have a tests/test_cli.py; importlib mode lets same-named
# test modules coexist without per-directory __init__.py files.
addopts = "--import-mode=importlib"
